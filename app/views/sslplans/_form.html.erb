<%= form_for(@sslplan) do |f| %>
  <% if @sslplan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sslplan.errors.count, "error") %> prohibited this sslplan from being saved:</h2>

      <ul>
      <% @sslplan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

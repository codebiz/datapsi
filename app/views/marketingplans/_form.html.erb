<%= form_for(@marketingplan) do |f| %>
  <% if @marketingplan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marketingplan.errors.count, "error") %> prohibited this marketingplan from being saved:</h2>

      <ul>
      <% @marketingplan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
